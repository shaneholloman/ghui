---
"@kitlangton/ghui": patch
---

Command palette now accepts an `owner/repo` or GitHub URL as a query and offers an inline "Open <repo>" command — no need to open the dedicated "Open repository…" modal first. Layout polish: scopes group correctly even when commands interleave, command subtitles fill the previously empty space between title and shortcut, and a spacer separates each section.
